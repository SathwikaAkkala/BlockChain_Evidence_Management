{
  "name": "cybercrime-contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy:fuji": "npx hardhat run scripts/deploy.js --network fuji"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.3.0",
    "hardhat": "^2.20.0"
  }
}
