{
  "name": "cybercrime-smartcontracts",
  "version": "1.0.0",
  "private": true,
  "description": "Smart contracts for Blockchain-Enabled Cybercrime Evidence System",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy:local": "npx hardhat run scripts/deploy.js",
    "deploy:fuji": "npx hardhat run scripts/deploy.js --network fuji"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.0",
    "hardhat": "^2.20.1"
  }
}
